interface:
  display_name: "Tutti Agent Workspace App"
  short_description: "Build agent Tutti apps with CLI surfaces"
  default_prompt: "Use $tutti-agent-workspace-app to create an agent-enabled Tutti workspace app repository with CLI/reference surfaces."

policy:
  allow_implicit_invocation: true
