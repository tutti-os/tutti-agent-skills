interface:
  display_name: "Tutti Workspace App Factory"
  short_description: "Create Tutti app packages and CLI surfaces"
  default_prompt: "Use $tutti-workspace-app-factory to convert this repository into a Tutti workspace app package with a CLI surface."

policy:
  allow_implicit_invocation: true
